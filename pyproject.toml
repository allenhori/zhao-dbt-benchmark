[project]
name = "zhao-dbt-benchmark"
version = "0.1.0"
description = "state:modified+ vs zhao diff on a ~1,000-model dbt project (DuckDB)"
requires-python = ">=3.10"
dependencies = [
    "duckdb>=1.1,<2",
    "dbt-core>=1.9,<2",
    "dbt-duckdb>=1.9,<2",
]
